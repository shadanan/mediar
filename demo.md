```python codeanim
delay.set(tap=0.01, end=0.5)
tap("n", modifiers=[Key.cmd])
write("cd ~/Developer/mediar/demo\n")
write("rm -f ../demo.cast && rm -rf target && mkdir target\n")
write("asciinema rec ../demo.cast\n")
write('export PROMPT="$ "\n')
delay.set(tap=0.05, end=2)
write("tree\n")
pause(3)
write("mediar link source/show target/\n")
tap(Key.enter)
tap(Key.enter)
tap(Key.enter)
tap(Key.enter)
pause(3)
write("tree\n")
pause(8)
tap("d", modifiers=[Key.ctrl])
tap("d", modifiers=[Key.ctrl])
```
